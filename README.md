# use ansible operation server on-premises


## example

```
git clone this
cd ansible-onpre-srvset01
vi ssh_config
ansible-playbook site.yml --syntax-check
ansible-playbook site.yml --check
ansible-playbook site.yml

```
